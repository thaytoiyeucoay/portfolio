# Example environment variables for the AI portfolio
# Copy this file to `.env.local` and fill in your secrets.

# --- Hugging Face Inference (shared) ---
# Required. Your HF token for server-side calls (never expose on client).
HF_API_KEY=hf_xxx_your_token_here

# Optional. Timeout (ms) for HF API calls (default: 60000). Increase if you hit timeouts.
HF_API_TIMEOUT_MS=90000

# --- Image Captioning: Option B (Inference Endpoint) ---
# If you use a managed Hugging Face Inference Endpoint, set these:
# Base URL of your endpoint. Example:
# HF_API_BASE=https://abc-1234-xyz-east-1.aws.endpoints.huggingface.cloud
# Toggle endpoint mode. When set to 1, server posts directly to HF_API_BASE (no /models/<id> suffix)
# HF_ENDPOINT_MODE=1
# Some endpoints require X-API-KEY instead of Authorization: Bearer. If so, set this:
# HF_ENDPOINT_X_API_KEY=your_endpoint_x_api_key
# Some endpoints only accept JSON with base64 input. If you see 404/415 with raw bytes, enable this:
# HF_IMAGE_CAPTION_JSON=1

# --- Emotion Detection (Text Classification) ---
# Uses the same HF_API_KEY and timeout above via /api/hf-text-classify
# No extra env required unless you want to tweak timeout.

# Notes
# 1) After editing `.env.local`, restart the dev server.
# 2) Do NOT commit real secrets. This `env.example` is safe to version.

# --- Web Search Integration ---
# Option A: Google Custom Search (recommended)
# Create a Custom Search Engine (Programmable Search Engine) and set site scope as needed (or entire web if enabled).
# https://programmablesearchengine.google.com/
# API key: https://developers.google.com/custom-search/v1/introduction
GOOGLE_SEARCH_API_KEY=
GOOGLE_SEARCH_CX=

# Option B: Tavily (fallback)
# https://tavily.com/ — used when Google keys are not provided
TAVILY_API_KEY=
